# TODO - Mobile responsive + speed improvements

## Steps

- [x] Update `src/components/Navbar.jsx` to have robust mobile slide-in menu and better touch/spacing.
- [x] Optimize images for speed in `src/components/ProductCard.jsx`.
- [x] Optimize images for speed in `src/components/Slider.jsx` and `src/components/Banner.jsx`.
- [x] Optimize images for speed in `src/app/page.js` (pending).

- [x] Improve global mobile styles in `src/app/globals.css` (tap targets, prevent horizontal overflow).

- [ ] Run `npm run dev` and verify mobile layout (navbar, pages, product grid).
